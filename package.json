{
  "name": "rollup-bundler",
  "version": "1.0.0",
  "description": "The small training example demonstrationg the use and working of the module bundler.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "rollup --config --watch",
    "dev": "rollup src/dynamicLoading -f cjs -d dist",
    "plugins": "rollup --config --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^4.21.2"
  },
  "dependencies": {
    "rollup-plugin-css-only": "^4.5.2"
  }
}
